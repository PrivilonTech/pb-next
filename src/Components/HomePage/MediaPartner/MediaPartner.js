import React from "react";
import { Box, Typography } from "@mui/material";

export default function MediaPartner() {
  return (
    <Box sx={{ display: "flex", flexDirection: "column" }}>
      <Typography
        sx={{ textAlign: "center", color: "#787878", fontSize: "1.5rem" }}
      >
        Media Partners
      </Typography>
    </Box>
  );
}
